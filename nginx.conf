worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile on;

    server {
        listen 8080;
        server_name _;

        root /var/www/html;
        index index.php;

        rewrite ^/api/auth/login/?$ /pages/login.php last;
        rewrite ^/api/auth/logout/?$ /pages/logout.php last;
        rewrite ^/api/auth/callback/?$ /pages/callback.php last;
        rewrite ^/profile/?$ /pages/profile.php last;

        location / {
            try_files $uri $uri/ =404;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_index index.php;
        }

        location ~ /\.ht {
            deny all;
        }
    }
}